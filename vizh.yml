# Set this to true if you want debug logs.
#
# These can be VERY verbose, so we suggest turning them off
# unless you really need them.
debug: false

# Runtime configuration
# Using Bun for ultra-fast TypeScript execution
runtime: bun-1.1
